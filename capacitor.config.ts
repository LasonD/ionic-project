import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter.12315',
  appName: 'ionic-project',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
